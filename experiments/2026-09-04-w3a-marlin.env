# VERDICT 2026-09-04 05:20: MEASURED, CLOSED — boots and is correct; decode within noise of the Triton
# hybrid (structured +1%, c4 flat, @1400 +2%, code +3%, prose -2..-7%), prefill -6%. Not promoted
# (bench-results/2026-09-04-night-windows.md).
# W3a — Marlin FP8 fused-MoE backend (night of 2026-09-04). The image lists MARLIN among the
# potential Fp8 MoE backends; Marlin is a small-batch (decode) oriented GEMM. Boot-and-see:
# block-quantized [128,128] FP8 experts may be refused at startup (shows within 15 min, then
# restart plain); if it boots, the sanity gate decides correctness before any number is read.
# The tuned Triton JSON mount from cluster.env stays but is unused by this backend.
# Compare against the production confirmation (Triton hybrid): bench-results/2026-09-04-w2c-moe-tuned.md.
# Rollback: ./tp4ctl restart without TP4_ENV.
EXTRA_VLLM_ARGS="--kv-cache-memory=17179869184 --moe-backend marlin"
