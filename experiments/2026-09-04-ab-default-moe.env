# VERDICT 2026-09-04 02:45: MEASURED, hybrid confirmed. The default config is slower on every
# single-stream axis in both passes (structured -10%, code -8%, @1400 -6%, prose -4%), four streams and
# prefill flat; the hybrid JSON stays in cluster.env (bench-results/2026-09-04-night-windows.md).
# A/B arm "default MoE config" (night of 2026-09-04): production recipe WITHOUT the hybrid
# GB10-tuned Triton fused-MoE JSON, on the same hosts (iommu.passthrough=1). Purpose: settle
# the -5% at 4 streams seen in the production confirmation of the hybrid file
# (bench-results/2026-09-04-w2c-moe-tuned.md) with two clean passes per arm on the same
# host state. Engine flags otherwise identical. Rollback: ./tp4ctl restart without TP4_ENV.
EXTRA_DOCKER_ENV=""
