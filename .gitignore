# Housekeeping
.DS_Store
__pycache__/
*.pyc
.ruff_cache/
.claude/settings.local.json
node/moe-tune/triton-cache/

# --- Site configuration: created by the user from the .example templates next to them,
# --- never committed. See README § Site configuration.
/cluster.env
# Per-node netplan directories (one per alias). The template and the shared/common
# material stay tracked.
node/etc/*/
!node/etc/common/
!node/etc/default/
node/etc/common/99-tp4-nopasswd
node/tp4-autostart.service
node/ssh-config
scripts/mirror-private-terms.txt
.claude/
